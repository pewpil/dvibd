version: "3.9"

networks:
  dvibd-net:
    driver: bridge

services:
  postgres:
    image: postgres:17
    container_name: dvibd-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: dvibd
      POSTGRES_PASSWORD: dvibd_dev_password
      POSTGRES_DB: dvibd
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    networks:
      - dvibd-net
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U dvibd -d dvibd"]
      interval: 5s
      timeout: 5s
      retries: 5
      start_period: 10s

  # pgadmin:
  #   image: dpage/pgadmin4:latest
  #   container_name: dvibd-pgadmin
  #   restart: unless-stopped
  #   environment:
  #     PGADMIN_DEFAULT_EMAIL: dvibd@localhost
  #     PGADMIN_DEFAULT_PASSWORD: dvibd_admin_password
  #     PGADMIN_CONFIG_SERVER_MODE: "False"
  #     PGADMIN_CONFIG_MASTER_PASSWORD_REQUIRED: "False"
  #   ports:
  #     - "5050:80"
  #   volumes:
  #     - pgadmin-data:/var/lib/pgadmin
  #   networks:
  #     - dvibd-net
  #   depends_on:
  #     postgres:
  #       condition: service_healthy

volumes:
  pgdata:
    driver: local
  # pgadmin-data:
  #   driver: local
