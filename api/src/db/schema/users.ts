// db/schema/users.ts
// Placeholder table definition for the users domain. Real columns will be added
// later; each query and column choice will be explained as we build them.

import { pgTable } from "drizzle-orm/pg-core";

// TODO: define columns (id, email, passwordHash, displayName, createdAt, ...).
// export const users = pgTable("users", {
//   id: uuid("id").primaryKey().defaultRandom(),
//   ...
// });

void pgTable;

export {};
