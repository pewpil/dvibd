// db/schema/index.ts
// Barrel file that re-exports every table definition. Drizzle's client and
// drizzle-kit both consume the combined schema from here.

// TODO: re-export each schema module, e.g.
// export * from "./users.ts";
export {};
