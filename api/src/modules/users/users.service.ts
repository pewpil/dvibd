// modules/users/users.service.ts
// Business logic for the users domain. Talks to the DB layer (Drizzle) and
// returns plain data to the controller. This is where Drizzle queries will live
// — each will be added and explained granularly as we build features.

// TODO: import { db } from "../../db/client.ts" and the users table, then add
// functions like createUser, findUserById, listUsers.
export {};
